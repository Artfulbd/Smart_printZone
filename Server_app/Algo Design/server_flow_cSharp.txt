user punches card
get id 
get time
send on thread
	do
	  request for printer			[API call {1}] (may take time)
	  (it will get printer details with wait time)
	  if false punch || print abort command
		break
	  sleep for wait_time
	while wait_time > zero

	prepare printer
	notifying about print job starting      [API call {2}]
	while all_doc_not_printed
	   printing document
	   if has printer problem
		notify server 			[API call {3}]
	   after printing one doc, notify server, with remaining doc count. [API call {4}]
								    
	get killed

thread returns	